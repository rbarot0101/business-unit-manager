# Snowflake connection configuration for S3 deployment
# This is loaded by the Hub and merged with metadata.json snowflake_config

[connections.snowflake]
account = "re18978.us-east-1"
user = "ETL_SYS_USR"
warehouse = "ETL"
database = "ODS"
schema = "PUBLIC"
